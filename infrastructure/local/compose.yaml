services:
  redis:
    image: redis:7-alpine
    container_name: youtube-mcp-server-local-redis
    ports:
      - "${LOCAL_REDIS_PORT:-6379}:6379"
    command: ["redis-server", "--save", "", "--appendonly", "no"]
